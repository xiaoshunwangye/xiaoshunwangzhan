# 2026-08-24 工作日志

- 修复项目封面视频黑边问题：将 `src/styles/globals.css` 中 `.project-cover-img` 与 `.project-cover-video` 的样式从 `object-fit: contain` 改为 `object-fit: cover`，并设置 `position: absolute; inset: 0; width: 100%; height: 100%`，使视频/图片铺满封面按钮区域。
- 重新构建并验证通过（`npm run build`）。
